#include "../network/network.h"
#include <cmath>

class Backpropagation {
    Network network;
    std::vector<float> pred;
    public: 
        std::vector<float> lossFunction(std::vector<float> &input,float actualVal) {float loss;}
};